{
  "name": "man-at-arms",
  "version": "2.0.0-vite",
  "description": "A Man-at-Arms' Life - 100 Years War (Vite Build)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "preview-dev": "vite build --base / && npx vite preview --outDir dist --base /",
    "build-stories": "node build-stories.js"
  },
  "dependencies": {
    "inkjs": "^2.3.2",
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
